# upCheck plugin configuration.
# systemd units that must be active (postgresql units are glob-resolved)
services: []
# URL probes; expected is the wanted HTTP status code
urls: []
#  - url: "https://example.com/health"
#    method: GET
#    expected: 200
#    timeout-seconds: 10
